{
    :id => :stac_dev_get_applog,
    :description => "dev asks for a single instance's application logs",

    # Note: a negative :inst_num value means the instance is chosen randomly
    :dev_actions => [
                  { :dev_action => :VMC_APPLOGS, :args => { :app_name => &APPNAME,
                                                            :inst_num => &INSTNUM } }
                ]
}


